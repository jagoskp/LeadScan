apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: leadscan-api-hpa
  namespace: leadscan
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: leadscan-api
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
